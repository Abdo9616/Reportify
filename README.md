- Install: npm install

- Copy .env.example -> .env and fill values

- Run: `node index.js`

- Commands are registered to GUILD_ID if provided (fast). Remove GUILD_ID to register globally (may take up to 1 hour).